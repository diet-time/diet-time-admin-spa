import { Breadcrumbs as MuiBreadcrumbs, Link, Typography } from '@mui/material';
import { Link as RouterLink, useLocation } from 'react-router-dom';
export function Breadcrumbs() { const parts = useLocation().pathname.split('/').filter(Boolean); return <MuiBreadcrumbs aria-label="Breadcrumb" sx={{ '& .MuiBreadcrumbs-separator': { mx: .5 } }}><Link component={RouterLink} to="/" underline="hover" color="inherit">Home</Link>{parts.map((part, index) => { const path = `/${parts.slice(0, index + 1).join('/')}`; const label = part === 'new' ? 'New' : part === 'edit' ? 'Edit' : part.replaceAll('-', ' ').replace(/\b\w/g, (m) => m.toUpperCase()); return index === parts.length - 1 ? <Typography key={path} color="text.primary" variant="body2">{label}</Typography> : <Link key={path} component={RouterLink} to={path} underline="hover" color="inherit">{label}</Link>; })}</MuiBreadcrumbs>; }

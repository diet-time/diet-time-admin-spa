import { Alert, Button, Card, CardContent, Grid, MenuItem, Stack, TextField, Typography } from '@mui/material';
import { addDays, format } from 'date-fns';
import { useState } from 'react';
export function PlanCalendarPage(){const [start,setStart]=useState(format(new Date(),'yyyy-MM-dd'));const [days,setDays]=useState(7);return <Stack spacing={3}><Typography variant="h1">Plan calendar preview</Typography><Card><CardContent><Stack direction={{xs:'column',sm:'row'}} gap={2}><TextField type="date" label="Start date" value={start} onChange={e=>setStart(e.target.value)} InputLabelProps={{shrink:true}}/><TextField select label="Preview duration" value={days} onChange={e=>setDays(Number(e.target.value))}>{[7,12,20].map(v=><MenuItem value={v} key={v}>{v} days</MenuItem>)}</TextField><Button variant="outlined">العربية</Button></Stack><Grid container spacing={1.5} mt={2}>{Array.from({length:days},(_,i)=><Grid size={{xs:12,sm:6,md:3,lg:2}} key={i}><Card variant="outlined" sx={{boxShadow:'none'}}><CardContent><Typography fontWeight={800}>Day {i+1}</Typography><Typography variant="caption">{format(addDays(new Date(start),i),'EEE, dd MMM')}</Typography><Typography color="text.secondary" mt={2}>Slots load from the selected template.</Typography></CardContent></Card></Grid>)}</Grid><Alert severity="info" sx={{mt:2}}>Meal images and availability warnings appear after a template is selected from the API.</Alert></CardContent></Card></Stack>}
